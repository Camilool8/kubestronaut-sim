apiVersion: v1
kind: Service
metadata:
  name: nova-api
  namespace: draco
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: http-api
  selector:
    app: nova-api
  type: ClusterIP
