apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Production renders into its own Namespace; the base it builds on:
namespace: scutum
resources:
  - ../../base

# TODO: production runs a newer image tag, three replicas, and stamps
# env: prod onto everything this overlay renders — the Pods included.
