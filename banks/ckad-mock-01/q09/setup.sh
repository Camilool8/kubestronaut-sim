#!/usr/bin/env bash
set -euo pipefail
# Nothing to seed in the cluster: this question is entirely about the
# container tooling on the instance. The build context is delivered by
# q09/files/, which the instance entrypoint copies into /opt/course/9.
exit 0
