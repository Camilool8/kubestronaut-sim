apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# The base this overlay builds on. Everything else is up to you.
resources:
  - ../../base

# TODO: add a name prefix, a common label, an image override and a
# replica count so that `kubectl kustomize .` renders what the question
# asks for. Do not edit anything under base/.
