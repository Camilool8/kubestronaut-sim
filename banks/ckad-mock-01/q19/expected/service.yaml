apiVersion: v1
kind: Service
metadata:
  name: inventory
  namespace: serpens
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 8080
  selector:
    app: inventory
  type: ClusterIP
