In a Kubernetes cluster, every other component reads and writes cluster state through a single control plane component, which is also the only component that communicates directly with etcd.

Which component is this?
