On each worker node, one Kubernetes component registers the node with the cluster, watches for Pods assigned to that node, and makes sure the containers described in those Pod specs are actually running and healthy.

Which component does this?
