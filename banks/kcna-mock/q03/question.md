Kubernetes separates cluster components into the control plane, which makes global decisions about the cluster, and node components, which run on every node to support workloads.

Which of the following are control plane components? Choose all that apply.
