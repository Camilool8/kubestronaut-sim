A team runs a web application in a Pod. They want a second container in the same Pod that continuously collects and ships the application's logs, starting alongside the app and running for the Pod's entire lifetime.

In Kubernetes terminology, what is this log-shipping container called?
