A team is deploying a replicated database on Kubernetes. Each replica needs a stable, unique network identity (so peers can find each other by name) and its own persistent storage that stays attached to the same replica even after rescheduling.

Which workload kind should they use?
