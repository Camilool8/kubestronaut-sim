A CI system needs to attach non-identifying metadata — such as a build number, a Git commit SHA, and a link to the pipeline run — to a Deployment object. This metadata is for humans and tools to read, and it must not be used to select or group objects.

Which Kubernetes mechanism is intended for this?
