Two teams share a single Kubernetes cluster. Each team wants to create its own Deployment named `web`, and neither team's resources should collide with the other's.

Which Kubernetes feature lets both `web` Deployments coexist in the same cluster?
