How many sides does a triangle have?

This is a smoke-test fixture question. It is deliberately trivial: the
answer must be obvious so a human debugging the smoke run can spot a
grading inversion at a glance.
